# spread-simulator — 디지털 공간에서 정보의 확산 속도 체험하기

> 한 줄 소개: **교과서의 SNS 관계망 그림을 그대로 옮겨, 개인 정보가 몇 분 만에 몇 명에게 퍼지는지 눈으로 보는 시뮬레이터**

## ① 목적과 대상 단원

- **단원**: 중학교 정보 5단원 **디지털 문화**
- **성취기준**: [9정05-02] (정보 윤리·사이버 폭력 예방), [9정05-03] (개인 정보·권리 보호)
- **출처 활동**: 교과서 「해 보기 1 — 디지털 공간에서 정보의 확산 속도 체험하기」
  중 **오른쪽 원형 관계망 그림**(사용자가 "동그라미 부분"이라고 지목한 부분)
- **활동 목표**: 디지털 공간에서 정보가 얼마나 빨리 확산되는지 체험할 수 있다.

교과서 문항 (1)(2)(3) 의 예시답안 **700명 / 3,100명 / 3,800명** 이
앱에서 그대로 나오는 것이 이 앱의 정확성 기준이다.

### 교사용 참고자료(파란 글씨) 반영
| 교과서 지도상의 유의점 | 앱에서 반영한 곳 |
|:---|:---|
| 사람 모양은 팔로워 수 100명을 나타내며, **팔로워가 많을수록 확산 속도가 더 빠르다** | ③ "팔로워가 더 많다면?" — 50/100/200/500명으로 바꾸면 **1분당 확산 속도(명/분)** 가 같은 비율로 커지는 것을 보여 준다 |
| 1·2차로 표현했지만 **실제로는 더 많은 단계를 거쳐 빠르게 확산**된다 | ③ "계속 퍼진다면?" — 5차까지 누적 표 (700 → 3,800 → 17,500 → 78,200 → 347,000) |

### 교과서 2번(사이버 폭력 사례 조사·발표)은 **일부러 넣지 않았다**
사용자가 범위를 "1번 확산 시뮬레이션만"으로 정했다. 나중에 넣는다면
개인정보를 저장하지 않는다는 규칙에 맞게 **화면에만 띄우고 저장하지 않는 표**로 만들 것.

## ② 기능 목록

| 영역 | 기능 |
|:---|:---|
| 머리말 (1단계) | **학생이 직접 게시물을 눌러 올린다.** 올리기 전에는 ①②③이 아예 보이지 않는다. 올리면 해시태그의 개인 정보에 표시가 붙고 「사진 한 장에 이만큼이 담겨 있었어요」 목록이 나타난다 |
| ① 퍼져 나가는 모습 | **도는 지구본** — 한국(서울)에서 시작해 **육지를 따라** 4차(782명)까지 전 세계로 퍼진다. 선 위에 「N분」이 드문드문 |
| | 지구는 **확산이 향하는 쪽으로** 돌고, **끌어서 직접 돌려 볼 수도** 있다 (가로=회전, 세로=기울기) |
| | ▶재생 / ⏸멈춤 / ↺처음으로 / **⏭10분으로** / **🌐각도 되돌리기**, 배속 0.5·1·2·4배, 시간 슬라이더(0~12분) |
| | 계기판 — 경과 시간 · **본 사람 수(큰 숫자)** · 단계별 1~4차 · **교과서 기준(1·2차)** · 확산 속도 · 배지 |
| ① 문제 풀기 | 교과서 (1)(2)(3) 그대로. **그림 설명 글**을 먼저 읽고 예상해서 답을 쓴다. `확인` · `💡힌트` (해설 보기 버튼은 없앴다). `7`처럼 **점 개수만 센 답을 따로 알아채서** "×100 해 보세요"로 안내 |
| ③ 더 알아보기 | 팔로워 수 바꾸기 / **글 삭제 체험** (두 가지뿐이다 — 아래 참고) |

### "글 삭제" 체험이 이 앱에서 가장 중요한 학습 장치 (2026-08-01 보강)
확산 도중 `🗑️ 홍길동이 글 삭제하기`를 누르면 홍길동에 🚫 표시가 붙지만
**본 사람 수는 1도 줄지 않고, 그 뒤에도 계속 늘어난다.**
→ [9정05-03] 의 "디지털 공간에 한 번 올린 정보는 완전히 지우기 어렵다"를 몸으로 겪게 하는 부분이다.
**모델을 단순화한 게 아니라 일부러 이렇게 만든 것이니, 나중에 "삭제하면 확산이 멈춰야 하지 않나?"
라고 고치지 말 것.**

사용자 요청(*"버튼을 누르면 지운 뒤에도 확산되고 퍼간 사람이 있으니 0명보다 더 많은 사람이
계속 보고 계속 확산해 나갈 거야"*)에 따라 두 가지를 더 넣었다.

1. **누르면 자동으로 재생이 이어진다** (`if (!S.playing && S.t < END_T) play()`).
   멈춘 채로 두면 "지운 뒤 0명" 으로 보여서 **삭제가 통한 것처럼 오해**하기 때문이다.
2. **숫자가 아직 0일 때는 0을 쓰지 않는다.** `지금도 퍼지는 중… 곧 숫자가 올라갑니다` 로 바꾸고,
   1명이라도 늘면 `지운 뒤에 더 본 사람 8,600명 … 계속 늘어나는 중`(깜빡이는 안내) 으로 바뀐다.
   확산이 다 끝나면 "계속 늘어나는 중" 이 사라지고 최종 인원만 남는다.
   → **문구에서 `0명` 이 그대로 보이게 되돌리지 말 것.** 이 활동의 결론이 뒤집힌다.

### 「계속 퍼진다면?」 단계별 누적 표는 **삭제했다** (2026-08-01)
사용자 지시로 ③의 세 번째 블록(1~5차 누적 표 + 막대 + ※ 설명)을 통째로 없앴다.
①의 그림이 이미 4차까지 그리고 고리마다 인원을 보여 주므로 표가 겹치는 정보였다.
`spread-core.js` 의 `growthTable()` 과 그 단위 시험(12개)도 함께 지웠다 —
다시 넣을 일이 생기면 이 문단을 지운 커밋에서 되살릴 것.

## ① 그림 = 도는 지구본 (2026-08-01 교체) — 여기부터 읽을 것

사용자 지시: *"수업용화면에 안B지구본으로 교체해줘"*
→ **교과서식 동심원 그림을 버리고, 시안 페이지(`alt-views.html`)의 안 B 지구본을 본 앱에 옮겼다.**
예전 그림에 있던 것들(고리·줌아웃·사람 모양 아이콘·사람 위의 「몇 차」 숫자·고리 이름표·
1차 확산자 클릭해 무리 보기)은 **전부 없어졌다.** 되살리려면 이 문단 이전 커밋을 볼 것.

| 단계 | 사람 수 | 서울에서 몇 도 | 다 보는 시각 |
|:---|:---|:---|:---|
| 1차 | 7명 | 약 48° | 4분 |
| 2차 | +31명 = 38명 | +45° | 9분 |
| 3차 | +137명 = 175명 | +38° | 10분 |
| 4차 | +607명 = 782명 | +32° | 11분 |

`S.stages` 는 `C.MAX_STAGES`(4) 로 고정이다. `spread-core.js` 는 여전히 2~4차를 다 만들 수 있고
**단위 시험도 그대로 남겨 두었다** — 화면에서만 4차로 고정한 것이니 core 쪽을 정리하지 말 것.

### 지구본이 지켜야 할 것 (시안 단계에서 하나씩 요청받아 굳힌 규칙)
- **출발은 한국(서울 37.5N/127E).** 출발점에 글씨는 붙이지 않는다 — 가운데 민트색 점이 한국이다.
- **사람은 육지에만 앉는다.** 후보 지점이 `onLand()` 를 통과할 때까지 방위·거리를 다시 뽑는다
  (① 부모 방향에서 90번까지 넓혀 가며 → ② 부모 둘레를 6도씩 한 바퀴 → ③ 최후에만 부모 옆).
  **이 재시도 루프를 빼면 태평양 한가운데에 사람이 찍힌다.**
- **퍼지기 전 선·점은 path 문자열에 아예 넣지 않는다.** 희미하게라도 미리 보이면 안 된다.
- **뒷면(z≤0)인 사람·선은 그리지 않는다.** 지구가 돌면 다시 나타난다.
- **「N분」은 드문드문만** — 1차 7개 전부 + 2차는 넷에 하나 = 15개(782개 선 중).
  3·4차는 전부 1분이라 뺐다. 양쪽 끝이 다 앞면일 때만 보인다.
- **대륙 외곽선은 변마다 `SUB`(8) 등분해서 그린다.** 꼭짓점만 지평선으로 밀면 해안선이 찌그러져
  **육지에 있는 사람이 바다에 있는 것처럼** 보인다(실제로 83%까지 떨어졌다).
- **유라시아 외곽선의 한반도 부분은 손대지 말 것.** 뾰족한 홈이던 시절 **서울이 바다로 판정**됐다.
- 세계 지도(`LAND`)는 아주 단순화한 그림이다. **지리 수업용 정확한 지도가 아니다.**

### 지구가 도는 방식 (`spreadLon`)
`회전 = 퍼진 만큼 꾸준히(SWEEP 250°) + 확산이 쏠린 쪽으로 조금(GAIN 1.1)`

- 0분에는 평균이 곧 서울이라 **한국이 정면에 멈춰 있다** — 별도 처리가 필요 없다.
- 시각 `t` 만으로 정해지므로 슬라이더를 끌어도 늘 같은 각도가 나온다.
- `sharp`: 사방으로 고르게 퍼지면 평균 벡터가 0에 가까워져 방향이 튄다. **벡터가 짧으면 기울이지 않는다.**
- ❌ **"최근에 본 사람만 크게 치는(exp 가중치)" 방식으로 바꾸지 말 것.** 갈래마다 방향이 달라
  0.25분마다 화면이 388px 씩 튀었다.
- ❌ **`SWEEP` 을 빼고 방향만 쓰지 말 것.** 전 세계로 퍼지면 방향이 사방이라 화면이 심하게 흔들린다.
- 회전이 매끄러운지 재려면 `spreadLon(t)` 을 0.25분 간격으로 훑어 **한 걸음이 12도를 넘는지** 보면 된다.

### 지구본 끌어서 돌리기
- `proj(lat, lon, lam0, phi0)` 가 일반 정사영이다. `phi0` 는 위아래 기울기(0이면 북극이 위).
  `S.dragLon`/`S.dragLat` 이 자동 회전 **위에 더해진다.**
- 가로로 끌면 돌고, 세로로 끌면 기울어진다(±75도). `pointer` 이벤트라 터치도 된다
  (`#net { touch-action: none }` 이 있어야 화면이 같이 스크롤되지 않는다).
- `🌐 각도 되돌리기` 와 `↺ 처음으로` 둘 다 `dragLon`/`dragLat` 을 0으로 되돌린다.

### 3차부터 선의 시간은 모두 1분
`DEEP_EDGE_MIN`. 교과서 그림의 가장 빠른 연결이 1분이라 "이 속도로 계속 퍼진다면"의 기준으로 삼았다.
인원은 비율 31÷7 ≈ 4.4 로 이어서 만들되, 앞사람들에게 고르게 나눠 주고 앞에서부터 한 명씩 더 맡게 해서
합을 정확히 맞춘다(3차 137 / 4차 607).

### 교과서 정답이 흔들리지 않게 막아 둔 곳 (중요)
화면이 4차까지 보여 줘도 **문제 (1)(2)(3)은 언제나 2차까지만 세어 채점한다.**

- `answers()` 가 `countAt(..., TEXTBOOK_STAGES)` 로 **2차까지만** 센다. → 4차 화면에서도 700/3,100/3,800.
- 계기판에 `그중 교과서 기준(1·2차)은 3,800명` 줄(`#tbLine`)이 함께 나온다.
- ② 문제 위에 노란 안내(`#quizStagesNote`)가 나온다.
- ⚠️ **지구본에서는 학생이 점을 세어 7·31 을 알아내기 어렵다**(뒷면으로 넘어가 안 보이는 점이 있다).
  그래서 **세는 일은 교과서 그림에 맡기고**, 앱은 문제를 먼저 풀게 한 뒤 확산 그림으로
  확인하게 하는 순서를 쓴다(위 "예상 → 확인" 참고). 앱 안에서 세게 하고 싶으면
  ① 문제 옆에 작은 정지 그림(동심원)을 하나 넣는 것이 가장 간단하다.

**나중에 이 앱을 고칠 때 `answers()` 에서 `TEXTBOOK_STAGES` 를 빼지 말 것.** 빼면 화면 설정에 따라
교과서 정답이 바뀌어 버린다.

### ⚠️ 성능 — 점·선을 단계별 `path` 하나에 몰아 그린다
사람 783명 + 선 782개를 각각 element 로 두면 지구가 돌 때마다 1,565개의 좌표를 다 고쳐야 해서 느리다.
지금은 **단계별로 `path` 하나씩**(점 5개 + 선 4개 = element 9개)에 몰아 그린다 →
**한 장면 4.3ms**. circle/line 여러 개로 되돌리지 말 것.
`MAX_STAGES = 4` 도 같은 이유다(5차는 2,688명이라 무리).

## 단계별 진행 (2026-08-01) — 화면이 처음부터 다 보이지 않는다

사용자 요청: *"단계별로 체험할 수 있게 해보자. … 학생들이 사진을 게시하도록 첨부한 사진만큼만
보이게 해. 인스타 게시모양을 클릭하게 해. 그러면 밑에 퍼져나가는 모습 보기가 나타나는걸로."*

### ⚠️ 활동 순서는 「예상 → 확인」이다 (2026-08-01 변경)
사용자 지시: *"시뮬레이션 확산보기를 하기 전에 … 문제를 먼저 풀어보고 확산보기를 통해
얼마나 확산되는지를 확인해보는 활동 순서로 하는게 좋을 것 같아."*
→ **문제(①)가 확산 그림(②)보다 앞에 온다.** 순서를 되돌리지 말 것 — 그림을 먼저 보면
계기판에 답이 다 나와 있어서 문제가 "베껴 쓰기"가 된다.

| 단계 | 여는 조건 | 열리는 것 |
|:---|:---|:---|
| 1-a 개인 정보 **찾아보기** | 처음 화면 | 머리말 + 게시물만 (`.stage-locked` 로 ①②③ 감춤). 해시태그를 눌러 고른다 |
| 1-b 게시 → **정답 공개** | 「📤 게시하기」 (`publishPost`) | 태그 채점 결과 + 누출 목록(`#leakBox`) + 「다음으로 넘어가기」 버튼 |
| 2 문제 풀기 **(예상)** | 「다음으로 넘어가기」 (`goNext`) | ① 문제. **확산 그림은 아직 안 열린다** |
| 2-b 버튼만 등장 | **세 문제에 모두 답을 써 본다**(맞든 틀리든, `readySim`) | 「확산 보러 가기」 버튼. **그림은 아직 안 열린다** |
| 3 퍼지는 모습 **(확인)** | **그 버튼을 누른다** (`openSim`) | ② 확산 그림, 그쪽으로 스크롤 |
| 4 더 알아보기 | **10분까지 확산을 본다** (`t >= DEADLINE` → `unlockDeep`) | ③ |

- 2-b 조건은 **정답이 아니라 "답을 써 봤는가"** 다(`S.answered` 에 3개가 차면 버튼이 뜬다).
  정답을 요구하면 막힌 학생이 다음으로 못 넘어간다.
- **⚠️ `readySim()` 에서 확산 그림을 열지 말 것.** 처음엔 세 문제를 다 풀면 바로 그림이
  열리게 했다가, *"3번 답을 확인하고 확산 보러가기를 클릭하면 나오게 해줘"* 라는 지적을 받았다.
  마지막 문제의 채점 결과를 볼 틈도 없이 다음 화면이 튀어나왔기 때문이다.
  그래서 `S.simReady`(버튼 등장)와 `S.simOpen`(그림 열림)을 따로 두었다.
### ① 문제 화면 = 위(그림 + 설명) / 아래(문제 세 개)
- **위 `.quiz-top`** — 왼쪽에 교과서 관계망 그림(`#tbFig`), 오른쪽에 그림 읽는 법(`.diagram-note`)
- **아래 `.quiz-qs`** — 문제 세 개 + 잠김 안내 + 「확산 보러 가기」 (**전체 폭**)
- 처음엔 「그림+설명 | 문제」 로 좌우 배치했는데 **그림이 너무 작다**는 지적을 받고
  위쪽을 넓게 쓰도록 바꿨다(그림 245px → 361px). 문제는 아래 전체 폭으로 폈다.
- 글자 크기는 **설명 19/18px(1pt 키움) · 문제 18px(한 단계 줄임)**,
  답 입력 칸은 120×39px, **`placeholder` 는 없앴다**(사용자 요청).

### 교과서 관계망 그림 (`buildTextbookFigure`)
사용자 요청: *"설명 글 옆에 원래 교과서 이미지 넣어줘"*

- 이미지 파일이 아니라 **`spread-core.js` 의 관계망 자료로 그린 정지 SVG** 다.
  그래서 교과서와 같은 7명·31명이 나오고, 자료를 고치면 그림도 따라 바뀐다.
- 확산 그림(지구본)과 **완전히 별개**다. 여기서는 사람 모양과 선을 다 그려서
  **학생이 7명·31명을 직접 셀 수 있게** 한다(지구본에서는 셀 수 없다).
- 종이처럼 밝은 바탕(`.tb-bg`)에 검은 사람(1차)·흰 사람(2차) — 교과서 그대로다.
- 「N분」 글씨는 2차 선에서 **안쪽/바깥쪽 세 단(0.36/0.56/0.76)으로 엇갈리게** 놓는다.
  한 값이면 겹친다. 실측 결과 **글씨끼리·사람끼리·글씨-사람 겹침 모두 0**.

- **⚠️ ① 문제 위 「그림 설명」(`#quizIntro`, `.diagram-note`)을 지우지 말 것.**
  확산 그림보다 문제가 먼저 나오므로, **그림 없이도 풀 수 있게 말로 다 적어 둔 곳**이다
  (안쪽 겹 7명 / 바깥쪽 겹 31명 / 사람 1개 = 팔로워 100명 / 선 위 숫자의 뜻 / 총시간 10분 미만).
  이게 없으면 학생이 7·31 을 알 길이 없어 문제를 풀 수 없다.
  문제 힌트도 계기판이 아니라 **이 설명 기준**으로 바꿨다(그때는 계기판이 아직 없다).
- **「해설 보기」 버튼은 없앴다**(사용자 지시). 남은 버튼은 `확인` 과 `💡 힌트` 둘뿐이다.
  정답을 맞히면 해설이 정답 문구에 함께 나오므로 따로 볼 필요가 없다.
- **⚠️ 게시와 다음 단계 열기를 한 번에 묶지 말 것.** 사용자가 명시적으로 나눴다 —
  *"게시버튼을 누르고 아래에 퍼져나가는 모습 보기 화면이 동시에 나타나지 말고,
  개인정보를 확인할 시간을 준 후에 다음으로 넘어가기버튼을 클릭하면"*.
  그래서 상태도 `S.posted`(게시함)와 `S.movedOn`(확인하고 넘어감) 두 개로 나뉘어 있다.
- **해시태그는 `<button class="tag">` 이다.** 개인 정보가 든 것에는 `data-leak="1"` 이 붙어 있다.
  올리기 전에는 눌러서 고르고(`.picked`), 올린 뒤 채점해 `.hit`(맞힘 ✓) / `.miss`(놓침) /
  `.wrong`(잘못 고름 ✕) 클래스가 붙는다. 정답인 빨간 물결 밑줄은 `body.posted` 가 붙어야 나온다 —
  **처음부터 표시되게 바꾸지 말 것.** 스스로 찾아보게 하는 것이 이 단계의 목적이다.
- 채점 문구는 세 가지다: 만점(`🎉 완벽해요!`) / 하나도 못 맞힘 / 부분 정답(찾은 수·놓친 수·잘못 고른 수).
- 열린 뒤에는 다시 잠기지 않는다(↺처음으로 를 눌러도 그대로). 수업 중 되돌아가기가 더 불편하다.
- 잠겨 있는 동안 ① 아래에 `#nextHint` 로 여는 조건을 알려 준다. 열리면 사라진다.

### 게시물 사진 — 외부 이미지 파일이 아니라 SVG 그림이다
「인생네컷」 4컷을 `index.html` 안에 **인라인 SVG** 로 직접 그렸다.
`<defs>` 에 두 인물(`#gA` 긴 머리·분홍 후드 / `#gB` 양갈래·민트 가디건)과 `#peace`, `#heart` 를
정의하고 `<use>` 로 4컷에 배치한다. 몸이 프레임 아래로 잘리는 건 `clipPath` 로 의도한 사진 크롭이다.

- **이미지 파일(.png/.jpg)이나 CDN 을 쓰지 않는 이유**: 이 앱은 인터넷 없이 열려야 하고
  `index.html` 하나로 배포되어야 한다. 사진을 바꾸고 싶으면 `#gA`/`#gB` 의 `fill` 색이나
  머리 모양 `path` 만 고치면 4컷 전체가 같이 바뀐다.
- 실제 학생 사진이 아니라 **단순한 도형 그림**이다. 수업 자료로 안전하게 쓰라고 일부러 이렇게 했다 —
  실사 이미지로 바꾸지 말 것.

### 해시태그의 개인 정보는 전부 가상이다
`#별빛중` `#2학년7반` `#15살` `#햇살아파트103동` `#금요일5시_별빛학원앞`
`#내번호010-1234-5678_디엠ㄱㄱ` — **지어낸 정보**이고, 화면에도
`※ 위 정보는 모두 수업용으로 지어낸 가상의 정보입니다.` 를 함께 띄운다.
**실제 학교 이름·주소·전화번호로 바꾸지 말 것.** 이 안내 문구도 지우지 말 것.

## 디자인 (2026-08-01 개편) — 컨셉을 먼저 읽고 고칠 것

사용자 요청: *"중학생 기준으로 디지털 윤리에 어울리는 디자인으로 변경해줘"*

> **화면 전체가 하나의 시뮬레이션 장비**
>
> (처음에는 "①만 어둡게, 나머지는 밝은 종이"로 만들었다가, 사용자가 *"화면은 전체적으로
> 시뮬레이션 화면 같이 위 아래도 어두운 화면이 좋을 것 같아"* 라고 해서 **전부 어둡게** 바꿨다.)

- **위에서 아래까지 전부 어둡다.** `.sim-card`(①)만 조금 더 깊은 색 + 푸른 테두리 빛으로
  "진짜 화면(스크린)"처럼 보이게 구분한다.
- **`:root { color-scheme: dark }`** — 슬라이더·드롭다운 같은 브라우저 기본 UI도 어두운 화면에 맞게
  나온다. 이거 하나로 해결되니 `::-webkit-slider-*` 같은 걸 따로 쓰지 말 것.
- **글자 크기 기준: 본문 21px.** (교실 TV 요청으로 19px → 21px 로 키웠다) h2 26 / 버튼 19 / 범례 16.
  더 줄이지 말 것.
- **⚠️ 한 단계 = 한 화면** (사용자 지적: *"화면을 100%로 보면 단계마다 한 화면에 안 들어온다"*).
  style.css 끝의 **「화면 한 컷에 들어오게 하는 조절」 블록**이 여백과 그림 높이를 줄인다.
  글자는 교실 TV용이라 거의 건드리지 않고 **여백 + 그림 높이**로 맞췄다.
  - 그림 세 개에 `max-height` 를 `vh` 로 걸어 **화면이 작을수록 그림이 먼저 줄어든다** —
    인생네컷 `19vh` / 교과서 그림 `38vh` / 지구본 `43vh`.
    **⚠️ 교과서 그림을 38vh 보다 키우면 문제 카드가 한 화면을 넘어간다.**
  - 조작 줄(버튼+슬라이더)은 좁은 오른쪽 칸에 두면 버튼이 세 줄로 접혀 길어져서,
    **`.sim-bar` 로 아래 전체 폭에 뺐다**(확산 카드 1024 → 867px).
  - **실측(창 1600×950 = 1080p 화면에서 브라우저 기본 상태)**
    — 머리말 937 / 문제 937 / 확산 867 / 더 알아보기 441 → **네 단계 모두 한 화면에 들어옴.**
    다만 문제에 답을 다 쓰면 채점 문구가 붙어 1,177px 가 된다(그때는 확산 그림으로 넘어가므로 무방).
    노트북(창 640px 이하)에서는 스크롤이 조금 생긴다.
  - 다시 잴 때는 창을 1600×950 으로 맞추고 각 카드의 `getBoundingClientRect().height` 를 보면 된다.
- **`body { word-break: keep-all }`** — 한글은 기본값이면 단어 한가운데서 줄이 바뀐다
  ("바꿔 보 / 자"). 어절 단위로만 줄이 바뀌게 하고, 끊을 수 없는 긴 낱말(해시태그 등)만
  `overflow-wrap: break-word` 로 예외 처리한다. **지우면 문장이 다시 어중간하게 잘린다.**
- **`select option` 에 불투명한 배경색을 직접 준다.** 반투명 배경(`rgba(255,255,255,.1)`)을
  쓰면 펼친 목록이 흰 바탕 + 흰 글씨가 되어 아무것도 안 보인다(실제로 겪은 문제).
- **확산 단계는 점의 색과 크기로 구분한다** — 홍길동(민트) · 1차(코랄) · 2차(앰버) ·
  3차(연앰버) · 4차(더 연한 앰버). 범례의 동그라미가 지구본 위의 점과 같은 색·비슷한 크기다.
  (교과서의 "검은 사람 / 흰 사람" 아이콘은 지구본으로 바꾸면서 없어졌다)
- **머리말이 SNS 게시물 카드(`.post`)** 다 — 프로필 원 + "방금 전 · 전체 공개" + 게시물 + ⚠️ 경고 줄.
  중학생이 바로 알아보는 화면으로 상황을 설명한다.
  ⚠️ 게시물 본문은 **"다른 사람의 개인 정보가 담긴 사진 1장"처럼 설명만** 한다.
  실제 사이버 폭력 문구를 흉내 내어 쓰지 말 것.
- 색: 인디고 `#5b5bd6`(디지털) · 코랄 `#ff4d6d`(확산·경고) · 앰버 `#ff9f1c`(2차) · 민트 `#12c2b0`(홍길동).
  **파스텔 "귀여운" 톤은 쓰지 않았다** — 개인정보 유출·사이버 폭력을 다루는 단원이라
  가볍게 보이면 안 된다 (형제 앱 `excel-picker` 와 일부러 다르게 갔다).
- **외부 글꼴(CDN)을 쓰지 않는다.** 이 앱은 인터넷 없이 열려야 한다는 성질을 지키려고
  구글 폰트를 일부러 안 넣었다 (`excel-picker` 는 Jua 를 쓴다 — 따라 하지 말 것).
- **글자 대비를 브라우저에서 실측해서 맞췄다.** 모든 본문 글씨가 WCAG AA(4.5:1) 이상이다.
  가장 아슬아슬한 것은 꼬리말 5.01, 게시물 시간 5.01. 그래서 `--c-mute`/`--d-mute` 는
  "그냥 흐린 회색"이 아니라 대비를 계산해 정한 값이다 — **더 흐리게 바꾸지 말 것.**

## 파일 구성

| 파일 | 설명 |
|:---|:---|
| `index.html` | 수업용 화면 전체 (한 페이지) |
| `alt-views.html` | **시안 비교용 별도 페이지** — 수업에 쓰는 화면이 아니다 (아래 참고) |
| `spread-core.js` | **계산 전부** — 관계망 데이터, 시각별 인원, 채점, 누적 표. DOM 의존 0 |
| `main.js` | SVG 그리기, 애니메이션, 이벤트 |
| `style.css` | 스타일 (교실 TV 대응: 본문 19px, 본 사람 수 58px) |
| `tests/run-tests.js` | 단위 시험 49개. `npm test` |
| `vite.config.mjs` | 포트 8083 + 일반 스크립트 복사 플러그인 (아래 참고) |

## `alt-views.html` — 확산 그림 시안 비교 페이지 (2026-08-01)

사용자 요청: *"이 확산되는 원 그림을 교과서 참고 없이 더 와닿게 다른 페이지에다가 별도로
그려줄 수 있어? 참고해서 바꿀 수 있으면 바꿔볼려고."* → **고르기 위한 시안**이지
수업용 화면이 아니다. 채택되면 `index.html` 로 옮기고 이 파일은 지우면 된다.

> **✅ 안 B(지구본)는 2026-08-01 에 채택되어 `index.html` 로 옮겨졌다.**
> 이 페이지는 **안 A(낯선 화면들)를 남겨 두는 용도**로만 유지한다. 본 앱을 고칠 때 여기까지
> 따라 고칠 필요는 없다 — 다만 지구본 코드가 두 곳에 있다는 점은 기억할 것.

- 주소: `http://localhost:8083/alt-views.html` (`index.html` 에서 링크는 걸지 않았다 —
  학생에게 보일 화면이 아니다). 파일 하나에 CSS·JS 를 다 담아 본 앱과 완전히 분리했다.
- **자료는 `spread-core.js` 를 그대로 쓴다** (`buildNodes(null, 4)` → 782명, 사람 1 = 100명).
  그래서 시안의 숫자·시간이 수업용 화면과 어긋나지 않는다. **이 페이지를 위해 core 를 고치지 말 것.**
- 빌드에 포함하려고 `vite.config.mjs` 의 `build.rollupOptions.input` 에 두 페이지를 넣었다.
  (그래서 CSS 파일 이름이 `index-*.css` → `main-*.css` 로 바뀌었다)

| 시안 | 그림 | 노린 것 |
|:---|:---|:---|
| **안 A · 낯선 화면들** | 휴대폰 782대 격자(40열). 퍼지는 **순서대로** 화면이 켜지며 인생네컷 미리보기가 뜬다. 1차 확산자 7칸만 민트 테두리 | "내 사진이 지금 **몇 대의 낯선 화면**에 떠 있는가". 아는 사람 7대 vs 모르는 사람 775대의 대비가 핵심 |
| **안 B · 도는 지구본** | **한국(서울)에서 출발**해 진짜 지구본 위로 퍼진다. 대륙이 그려진 지구가 1분에 30도씩 돌고, 뒤로 넘어간 사람은 사라졌다가 다시 나타난다 | "내가 올린 사진이 **지구 반대편까지** 몇 분 만에 간다" |

### 안 B 를 고칠 때 지켜야 할 것 (2026-08-01 사용자 요청)
- **사람마다 위도·경도를 갖는다.** 출발점 `ORIGIN`(서울 37.5N/127E)에서 구면 위로
  단계마다 `HOP`(48°/45°/38°/32°)씩 방위를 조금씩 틀며 뻗는다(`destPoint`).
  합이 약 163도라 **지구 반대편까지 닿는다** — 사용자 요청("뻗어나가는 걸 전 세계로")에 맞춘 값이니
  줄이면 다시 아시아 근처에 몰린다. 육지를 못 찾아 넓게 훑을 때도 **±160도를 넘기지 않는다**
  (넘기면 왔던 쪽으로 되돌아가 한곳에 뭉친다).
- **⚠️ 사람은 육지에만 앉는다** (사용자 지적: *"바다에는 사람이 없는데?"*).
  후보 지점이 `onLand()`(레이 캐스팅 점-다각형 판정)를 통과할 때까지 방위·거리를 다시 뽑는다.
  ① 부모가 향하던 쪽에서 70번까지 조금씩 넓혀 가며 찾고 ② 그래도 없으면 부모 둘레를 6도씩 한 바퀴
  훑는다(섬처럼 사방이 바다인 경우) ③ 최후에만 부모 옆에 둔다. **이 재시도 루프를 빼지 말 것** —
  빼면 태평양 한가운데에 사람이 찍힌다.
- **대륙 외곽선은 변마다 `SUB`(8) 등분해서 그린다.** 꼭짓점만 지평선으로 밀면(clamp)
  가장자리 해안선이 크게 찌그러져 **육지에 있는 사람이 바다에 있는 것처럼 보인다**
  (실제로 83%까지 떨어졌다. 잘게 쪼갠 뒤 99.8%로 회복).
- **유라시아 외곽선의 한반도 부분은 손대지 말 것.** 처음에는 뾰족한 홈이라 **서울이 바다로
  판정**됐다. 지금은 `[131,43],[130,37],[127,34],[125,38],[122,40],[121,38]` 로 반도를 제대로
  그려서 서울이 육지 안에 들어온다.
- **매 프레임 정사영(`proj`)으로 다시 그린다.** `z <= 0`(뒷면)인 사람·선은 **그리지 않는다** —
  돌면 다시 나온다.
- **⚠️ 지구는 시계처럼 도는 게 아니라 「확산이 향하는 쪽」으로 돈다** (사용자 요청:
  *"처음부터 회전할 필요 없이, 퍼져나가는 속도로"* → *"확산되는 방향으로 지구가 돌면 좋겠다"*).
  `spreadLon(t)` 이 **지금까지 본 사람 전체의 구면 평균 위치**를 구해 그 경도 쪽으로 화면을 돌린다.
  0분에는 평균이 곧 서울이라 한국이 정면에 멈춰 있다 — 별도 처리가 필요 없다.
  시각 `t` 만으로 정해지므로 슬라이더를 끌어도 늘 같은 각도가 나온다.
  - **회전 = `SWEEP × 퍼진비율` (뼈대) + `확산 쏠린 방향 × sharp × GAIN` (기울임).**
    전 세계로 퍼지게 바꾼 뒤로는 "확산 방향"이 사방이라 평균만 따라가면 화면이 심하게 흔들려서,
    꾸준히 도는 회전을 뼈대로 깔고 그 위에 방향을 살짝 얹는 방식으로 바꿨다.
  - `SWEEP`(250°) 퍼진 만큼 꾸준히 도는 각도 / `FADE`(5분) 평균에 스며드는 시간 /
    `GAIN`(1.1) 확산 쪽으로 기울이는 정도.
  - `sharp`: 사방으로 고르게 퍼지면 평균 벡터가 0에 가까워져 방향이 튄다. 벡터가 짧으면
    (방향이 뚜렷하지 않으면) 기울이지 않는다.
  - ❌ **"최근에 본 사람만 크게 치는(exp 가중치)" 방식으로 바꾸지 말 것.** 갈래마다 방향이 달라
    0.25분마다 화면이 388px 씩 튀었다.
  - **회전이 매끄러운지 재는 법**: `renderB` 가 `window.__lam0` 에 현재 각도를 넣어 둔다.
    0.25분 간격으로 훑어서 **한 걸음이 12도를 넘지 않으면** 부드럽다 (지금 최대 11.2도).

### 지구본 끌어서 돌리기 (2026-08-01)
사용자 요청: *"끝나고 나면 지구본을 돌려볼 수 있으면 좋겠어"* → 언제든 끌 수 있게 했다.

- `proj(lat, lon, lam0, phi0)` 가 **일반 정사영**이다. `phi0` 는 위아래 기울기(0이면 북극이 위).
  `S.dragLon`/`S.dragLat` 이 자동 회전 **위에 더해진다**.
- 가로로 끌면 돌고, 세로로 끌면 기울어진다(±75도로 제한). `pointer` 이벤트라 터치도 된다
  (`touch-action: none` 이 있어야 화면이 같이 스크롤되지 않는다).
- `🌐 각도 되돌리기` 버튼과 `↺ 처음으로` 둘 다 `dragLon`/`dragLat` 을 0으로 되돌린다.
- 확산이 다 끝나면 안내 문구(`#dragHint`)가 민트색으로 강조되고, 아래 문구도
  "이제 지구본을 끌어서 돌려 보세요" 로 바뀐다.
- **출발점에 글씨를 붙이지 않는다** (사용자 요청으로 「한국에서 시작」 라벨 삭제).
  가운데 민트색 점(`nd0`)이 한국이고, 설명은 그림 위 `desc` 문구가 대신한다.
- **⚠️ 성능: 점·선을 단계별 `path` 하나에 몰아 그린다** (점 5개 + 선 4개 = element 9개).
  1,565개를 각각 element 로 두면 매 프레임 좌표를 다 고쳐야 해서 느리다.
  지금은 **한 장면 1.45ms** — circle/line 여러 개로 되돌리지 말 것.
- **퍼지기 전 선·점은 path 문자열에 아예 넣지 않는다.** *"퍼지기 전에 선은 희미하게
  표시하지마"* → 희미하게라도 미리 보이면 안 된다.
- **「N분」은 드문드문만.** 1차 7개 전부 + 2차는 넷에 하나 = 15개(782개 선 중).
  3·4차는 전부 1분이라 뺐다. 양쪽 끝이 다 앞면일 때만 보인다.
- **인원 숫자판(`buildHud`)은 맨 마지막에 붙여 그림 위에 얹는다.** 뒤로 점이 지나가도 읽히도록
  불투명한 배경을 깔았다. 총 인원 + 단계별 4줄이 `render()` 에서 갱신된다.
- **세계 지도(`LAND`)는 아주 단순화한 그림이다.** 대륙 10덩이(유라시아·아프리카·북미·남미·
  오세아니아·인니/뉴기니·그린란드·일본·마다가스카르·영국)를 [경도, 위도] 다각형으로 손으로 찍었다.
  뒤로 넘어간 꼭짓점은 지구 가장자리로 밀어붙여(clamp) 자연스럽게 잘리게 한다.
  **지리 수업용 정확한 지도가 아니다** — "지구구나" 하고 알아보는 용도다.
- 흩어짐은 `Math.random` 이 아니라 고정된 식(`prand`)이라 **새로 고쳐도 모양이 같다.**
- 아래쪽 **규모 비교**(우리 반 25 / 전교생 500 / 콘서트장 5,000 / 야구장 25,000)는
  학교 사정에 맞게 `REFS` 배열만 고치면 된다.

## 관계망 데이터 — ⚠️ 다시 만질 때 반드시 읽을 것

`spread-core.js` 의 `NETWORK` 가 교과서 그림이다.

```
1차 확산자 7명 (A~G), 각자의 2차 확산자 5+4+5+4+5+4+4 = 31명
가장 늦게 보는 사람도 9분  →  10분 안에 38명 전원 = 3,800명
```

**선 위의 `N분` 숫자는 교과서 사진에서 정확히 읽을 수 없어(해상도 한계) 재구성한 값이다.**
재구성할 때 지킨 조건은 두 가지이고, 이 조건만 지키면 교과서와 답이 같다.

1. 1차 7명, 2차 31명 (예시답안의 700명·3,100명이 여기서 나온다)
2. **모든 사람의 (1차 시간 + 2차 시간)이 10분 미만** — 교과서 예시답안 (3)의 근거
   "연결된 선의 총시간이 10분 미만일 경우 확산자 수로 계산한다. 모두 10분 미만이기 때문에"

교과서 원본의 분 숫자를 정확히 알게 되면 `NETWORK` 의 `min`/`children` 만 고치면 된다.
정답·시험·표는 전부 이 데이터에서 자동으로 계산되므로 **다른 곳은 손댈 필요가 없다**
(단위 시험이 그것을 확인한다 — 예: "3차 추정 = 137명"처럼 값을 고정한 시험은 함께 고칠 것).

### 3차 이후는 추정값
1차 7명 → 2차 31명이므로 한 사람이 평균 `31 ÷ 7 ≈ 4.4`명에게 전한 셈으로 보고
`buildNodes()` 가 3차(137명)·4차(607명)를 이어서 만든다. **교과서 그림에 실제로 그려진 것은
2차까지**이고 3·4차는 이 비율로 늘린 추정이라는 점을 잊지 말 것.

## 왜 `vite.config.js` 가 아니라 `vite.config.mjs` 인가

이 앱은 **외부 의존성이 0개**라 `index.html` 을 더블클릭으로도 열 수 있게 만들었다.
그러려면 스크립트를 ES 모듈이 아닌 **일반 `<script src>`** 로 써야 한다(`file://` 에서는
ES 모듈이 CORS로 막힌다). 그래서

- `package.json` 에 `"type": "module"` 을 넣지 않았다 → 단위 시험도 `require` 로 읽는다
- 그 상태에서 vite 설정만 ESM 으로 쓰려면 확장자가 `.mjs` 여야 한다

### `copyPlainScripts` 플러그인이 필요한 이유 (실제로 겪은 문제)
`npm run build` 를 처음 돌렸더니 **`dist/` 에 JS가 하나도 없었다.**
vite 는 `type="module"` 인 스크립트만 번들에 넣기 때문이다
(빌드 로그의 `can't be bundled without type="module" attribute` 경고가 그 뜻 — 지금도 뜬다).
`vite.config.mjs` 의 작은 플러그인이 `spread-core.js`·`main.js` 를 **이름 그대로** `dist/` 에
복사해서, `dist/index.html` 의 `src="spread-core.js"` 가 그대로 맞도록 했다.

**스크립트 파일을 추가하면 `copyPlainScripts([...])` 목록에도 넣어야 한다.** 안 넣으면
개발 서버(`npm start`)에서는 잘 되는데 `dist/` 에서만 조용히 빠진다.

## ③ 현재 개발 상태

**상태: 10차 (2026-08-01) — 확산 그림을 **도는 지구본**으로 교체 + 활동 순서를
**「문제(예상) → 확산 그림(확인)」** 으로 바꿈.**
(그전: 3·4차 확산 + 자동 줌아웃, 단계 탭 통합, 화면 전체 다크 + 글자 확대,
누적 표 삭제 + 글 삭제 체험 보강, 학생이 직접 사진을 올리는 3단계 진행)

### 지구본 교체 뒤 실측 (2026-08-01)
- 1단계~3단계 흐름 그대로 동작 — 태그 6개 다 맞히면 `🎉 완벽해요!`, 게시 → 「다음으로」 → ① 열림,
  ⏭10분 → ②③ 열림.
- 10분 시점: 총 **75,000명** / 1차 700 · 2차 3,100 · 3차 13,700 · 4차 57,500 /
  `그중 교과서 기준(1·2차)은 3,800명` / 문제 3개 모두 `700`·`3,100`·`3,800` 정답 처리.
- 지구본: viewBox `-560 -560 1120 1120`, 대륙 10, 격자 있음, 점 path 5 · 선 path 4, 「N분」 15개.
- **육지 비율 3·6·9·12분 모두 100%.**
- 가로 끌기 → 보이는 점이 314→305 로 바뀌고, 세로 끌기 → 581(기울어져 더 보임),
  `🌐 각도 되돌리기` → 314 로 복귀.
- 5분에 글 삭제 → 🚫 표시 뜨고, 11분에 `지운 뒤에 더 본 사람 67,100명 (모두 78,200명)`.
- 팔로워 500명 → `점 1개가 500명이면 … 19,000명`. 한 장면 **4.32ms**, 가로 넘침 없음, 콘솔 오류 없음.

### 순서 바꾼 뒤 실측 (2026-08-01)
- 카드 순서 `① 문제 풀기 — 먼저 예상해 보기` → `② 퍼져 나가는 모습 보기 — 예상이 맞았을까?`
  → `③ 더 알아보기`. 처음에는 셋 다 감춰져 있고 단계 표시는 `1 사진 올리기`.
- 게시 → 「다음으로 넘어가기」 → **문제만** 열림(확산 그림은 그대로 잠김), 단계 `2 문제 풀기 예상`.
- 문제 **2개만** 답했을 때는 확산 그림이 안 열림 → 3개째에 열리고 「확산 보러 가기」 버튼 등장,
  잠김 안내(`#quizHint`)는 사라짐, 단계 `3 퍼지는 모습 보기 확인`.
- ⏭10분 → 더 알아보기 열림, 단계 `4 더 알아보기`, 75,000명.
- 채점은 그대로 — `700`→정답 / `31`→"×100 해 보세요" / `3800`→정답.

### 검증 내역
- **단위 시험 59개 통과** (`npm test`) — 교과서 예시답안 700/3,100/3,800, 마지막 사람 9분,
  모든 사람이 10분 미만, 시각별 인원(0·1·3·9·12분), 팔로워 50/100/200/500, 답 읽기(`"3,100명"`→3100),
  채점 4종(correct/icon/wrong/empty), 숫자 표시.
  **3·4차 관련 22개** — 176명/783명, 단계별 137·607명, 아이디 중복 없음, 3차 10분·4차 11분에 완료,
  단계 인자 범위 조정(1→2, 9→4), **4차까지 그려도 정답은 700/3,100/3,800 그대로**.
  (누적 표를 없애면서 관련 시험 12개는 71개 → 59개로 함께 제거)
- **브라우저 실측** (개발 서버 + 빌드된 `dist/` 양쪽):
  - 사람 39개 / 선 38개 / 분 글씨 38개가 정확히 그려짐.
  - **아이콘·글씨끼리 겹치는 곳 0곳**, 그림이 viewBox 안에 다 들어감(`getBBox` 로 전수 확인).
    그림이 차지하는 만큼만 보이도록 `viewBox` 를 `70 70 680 680` 으로 좁혀 놓았다
    (좌표 계산은 `main.js` 의 `VIEW` 대로 820×820 기준 그대로다 — **둘을 헷갈리지 말 것**).
  - ⏭10분 → `3,800명` / 1차 `700명` / 2차 `3,100명` / 배지 "⏰ 10분 경과", 39명 전원 켜짐.
  - 문제 채점 4종 실제 클릭 확인 (`700`→정답, `7`→"×100 해 보세요", `500`→오답, 빈칸→안내).
  - 1차 확산자 `E` 클릭 → "3분에 보았고, 연결된 2차 확산자는 5명(500명)" + 관련 7개만 진하게.
  - 4분에 글 삭제 → 10분까지 진행: "이미 1,500명이 봤고, 지운 뒤에도 2,300명이 더 봤습니다
    (모두 3,800명)" — **숫자가 줄지 않고 계속 늘어나는 것 확인.**
  - 팔로워 200명 → 7,600명 / 844명·분, 문제는 여전히 100명 기준으로 채점되는 것 확인.
  - `npm run build` 성공 → `dist/`(index.html + main.js + spread-core.js + css)를
    `npm run preview` 로 띄워 동일 동작 확인. 콘솔 오류 없음.
- **3·4차 실측** (2026-08-01 추가분):
  - 4차 기본 화면: 사람 783개(7·31·137·607) / 선 782개 / 가는 선 744개 / "N분" 글씨 38개.
  - **줌아웃이 단계에 맞춰 정확히 3칸으로 움직인다** — `viewBox` 가
    `70 70 680 680`(0~3분) → `-50 -50 920 920`(4분, 3차 시작) → `-190 -190 1200 1200`(5분, 4차 시작).
  - 10분 시점 4차 화면: 총 **75,000명** / 1차 700 · 2차 3,100 · 3차 13,700 · 4차 57,500,
    그 아래 `그중 교과서 기준(1·2차)은 3,800명` 줄이 함께 표시됨.
  - **2차(교과서)로 되돌리면 처음 화면과 완전히 같아진다** — 사람 39개, `viewBox 70 70 680 680`,
    "N분" 글씨 38개 다시 보임, 안내 문구 사라짐, 시각(7.0분)은 유지, ⏭10분 → 3,800명.
  - 3차 화면에서 ③ 표의 3차 줄이 `그림에 있음 / 13,700명 / 누적 17,500명` 으로 표시됨(그림과 일치).
  - 1차 확산자 `A` 클릭 → "이 사람에게서 퍼진 사람은 모두 156명", 진하게 표시된 노드 157개(A + 156 + 홍길동).
  - **성능**: SVG 요소 1,850개, 한 장면 그리기 평균 **1.6ms** (60fps 여유).
  - 점 간격 실측 — 3차 10.6단위(점 지름 9 → 안 겹침), 4차 2.5단위(점 지름 3.4 → 촘촘한 띠로 보임, 의도한 모습).
- **디자인 개편 뒤 재확인**:
  - 글자 대비 8곳 실측 — 전부 WCAG AA(4.5:1) 통과. 꼬리말 5.01 / 게시물 시간 5.01 /
    표 아래 설명 5.90 / 눈금·팁 6.85 / 단계별 라벨 7.30 / 범례 7.09 / 어두운 카드 제목 13.66.
  - 가로 스크롤 넘침 없음. 카드 4개 · 문제 3개 · 표 5줄 정상.
  - 기능 회귀 없음 — 4차 10분 75,000명(교과서 기준 3,800명 줄 함께 표시) → 2차로 되돌리면
    39명·`viewBox 70 70 680 680`·3,800명, 채점 정상(`700`→정답 / `31`→"×100 해 보세요"),
    5분 삭제 후 10분까지 "2,200명이 본 뒤였고 지운 뒤에도 1,600명이 더" 정상,
    팔로워 500명 → 19,000명·2,111명/분 + 문제 안내 노출.
  - `npm test` 71개 통과, `npm run build` 성공.
  - ⚠️ **색이 실제로 어떻게 보이는지는 스크린샷으로 확인하지 못했다** (아래 "확인하지 못한 것" 참고).
    색상값·대비는 `getComputedStyle` 로 실측했다.
- **숫자·이름표 실측** (2026-08-01 마지막 변경분):
  - 탭 1개 / 사람 783명 / 숫자 38개(값은 `1`,`2` 두 종류) / 고리 이름표 4개.
  - **0분에는 숫자 opacity 0, 켜지면 1** — 퍼져 나갈수록 하나씩 나타나는 것 확인.
  - 고리 이름표가 줌 배율과 상관없이 **늘 36×24px** (scale 1.000 → 1.765 로 거꾸로 커짐),
    **783개 아이콘 전부와 겹침 0건** (0·4·6분 세 시점 모두).
  - 숫자끼리 겹침 0건. 숫자의 세로 위치 = 사람 모양 위에서 **72%** 지점(몸통 한가운데) ✓.
  - 화면상 숫자 크기 — 3분(줌인) 1차 19px·2차 14px / 10분(완전 줌아웃) 1차 10px·2차 9px.
  - 기능 회귀 없음 — 10분 75,000명(교과서 기준 3,800명), 문제 3개 모두 정답 처리,
    `C` 클릭 시 141개 진하게, 4분 삭제 후 "지운 뒤에도 72,500명이 더", 팔로워 200명 → 150,000명.
    콘솔 오류 없음, `npm test` 71개 통과, `npm run build` 성공.
- **전체 다크 + 글자 확대 + 고리 인원 표시 뒤 재확인**:
  - **글자 대비 24곳 실측 — 전부 WCAG AA(4.5:1) 통과**, 가장 낮은 값이 5.01(단원 태그).
    문제 피드백 3종(정답 9.76 / 힌트 10.63 / 오답 6.72), 표 추정행 5.26, 꼬리말 6.44 등.
  - 고리 이름표 겹침 검사 — **0·3·5·10·12분 + 팔로워 500명("4차 287,500명")까지
    사람·이름표·홍길동 이름과 충돌 0건**, 화면 밖으로 잘리는 것도 없음
    (0·3분에 3·4차 이름표가 화면 밖인 건 그 고리 자체가 아직 화면 밖이라 정상).
  - 「N분」 글씨를 15/12.5px 로 키우고 안팎 번갈아 배치 → **글씨끼리·사람과·점과 겹침 모두 0**.
  - 가로 넘침 없음. 본문 21px / h1 40px / 큰 숫자 64px 적용 확인.
  - 고리 이름표 값이 계기판과 정확히 일치 — `1차 700명 / 2차 3,100명 / 3차 13,700명 / 4차 57,500명`.
  - 기능 회귀 없음(10분·문제 3개·초점·삭제·성장표), `npm test` 71개 통과, 빌드 성공, 콘솔 오류 없음.
- **글씨 고정 크기 적용 뒤 재확인** (가장 많이 잰 부분):
  - **0~12분 1분 간격 13시점 + 팔로워 50·200·500명 = 16가지 상황에서 겹침 0.**
    잰 항목: 「N분」끼리 / 「N분」-사람 / 「N분」-점 / 「N분」-숫자 / 숫자끼리 /
    이름표-사람 / 이름표-점 / 이름표-홍길동이름 / 이름표끼리 / 이름표-「N분」.
  - **글씨 크기가 줌과 무관하게 일정**: viewBox 680 → 760 → 1200 으로 바뀌는 동안
    1차분 22px, 2차분 17px, 숫자1 20px, 숫자2 16px **변화 없음**. (이름표만 45 → 33px)
  - 4분 시점에 4차 이름표 하나가 화면 밖인데, 그때는 4차 고리 자체가 화면 밖이라 정상.
  - 이 값에 이르기까지 브라우저에서 **엇갈림 위치 16조합 × 글씨 크기 5단계를 직접 돌려**
    겹침 0이 되는 조합을 찾았다(위 표의 값이 그 결과다).
- **누적 표 삭제 + 글 삭제 보강 뒤 확인**:
  - ③에 남은 블록이 「팔로워가 더 많다면?」·「글을 지우면 없던 일이 될까?」 둘뿐, 표 0개, 가로 넘침 없음.
  - 4분에 삭제 → **자동으로 재생이 이어지고**, 문구가
    `지금도 퍼지는 중… 곧 숫자가 올라갑니다 (모두 2,500명)` →
    `지운 뒤에 더 본 사람 8,600명 … 계속 늘어나는 중 (모두 11,100명)` →
    `지운 뒤에 더 본 사람 75,700명 (모두 78,200명)` 으로 바뀌는 것 확인.
    **어느 시점에도 "0명" 이 그대로 보이지 않는다.**
  - ↺처음으로 를 누르면 안내가 지워지고 삭제 버튼이 다시 살아나는 것 확인.
  - `npm test` 59개 통과, 빌드 성공, 콘솔 오류 없음.
- **단계별 진행 + 인생네컷 사진 확인**:
  - 처음 화면에서 ①②③ · 누출 목록 · 다음 버튼 · 채점 문구가 모두 `display:none`,
    단계 표시는 `1 사진 올리기●`. 태그 11개 중 `data-leak` 6개.
  - 태그 4개(정답 3 + 오답 1)를 골라 게시 → **①은 열리지 않고** 누출 목록과 다음 버튼만 열림,
    채점 `6개 중 3개를 찾았어요. 놓친 것 3개, 잘못 고른 것 1개`,
    `.hit` 3 / `.miss` 3 / `.wrong` 1 확인. 게시 뒤 태그를 더 눌러도 선택이 바뀌지 않음.
  - 6개를 다 고르면 `🎉 완벽해요! … 6개를 모두 찾았어요.` 로 나오는 것 확인.
  - 색(트랜지션을 끝낸 뒤 실측) — 일반 `rgb(138,180,255)` / 맞힘 `rgb(255,157,176)`+초록 테두리+✓ /
    놓침 `rgb(255,157,176)`+노란 "놓침" / 잘못 고름 회색+취소선+✕, 밑줄 `underline wavy`.
  - 「다음으로 넘어가기」 → ①이 열리고 버튼이 사라지며 단계가 `1 ✓ / 2 ●` 로 바뀜.
  - ⏭10분 → ②③이 열리고 `#nextHint` 가 사라지며 단계가 `1 ✓ / 2 ✓ / 3 ●` 로 바뀜.
  - 가장 긴 태그(`#내번호010-1234-5678_디엠ㄱㄱ`)가 269px 로 칸(370px) 안에 들어감, 넘침 없음.
  - 사진 SVG: 4컷 각각에 인물 2명 배치(`<use>` 17개), 몸이 프레임 아래로 잘리는 것은
    `clipPath` 로 의도한 크롭. 실제 렌더 크기 370×291px, 가로 넘침 없음.
- **화면 다듬기 (사용자 지적 3건)**:
  - 드롭다운 펼침 목록이 흰 바탕+흰 글씨로 안 보이던 문제 → `select`·`option` 배경
    `rgb(38,46,96)` / 글자 `rgb(238,241,255)` 로 실측 확인.
  - `word-break: keep-all` 이 본문·문제·범례·심화 전부에 적용된 것, `overflow-wrap: break-word`
    확인. **가로 넘침 0, 자기 칸 밖으로 삐져나온 글 0개**(모든 p·li·h·span 전수 검사).
  - 범례에서 「사람 모양 위의 숫자 1·2 = …」·「선 위의 숫자 = …」 두 줄 삭제(사용자 지시).
    남은 항목 8개, 팔로워 줄(`#legendFollowers`)은 그대로 동작.

### 확인하지 못한 것 (다음에 볼 것)
1. **`index.html` 더블클릭(`file://`) 실제 확인.** 구조상으로는 열린다 —
   ES 모듈·`fetch`·CDN·`crossorigin` 을 하나도 쓰지 않고 같은 폴더의 일반 스크립트와
   상대경로 CSS만 쓴다(전수 검색으로 확인). 다만 작업 환경의 미리보기 브라우저가
   `file://` 로 이동하지 못해 **직접 열어 보지는 못했다.** 한 번 더블클릭해 확인할 것.
2. **▶재생 애니메이션의 실제 움직임.** 미리보기 창이 화면에 표시되지 않으면
   `requestAnimationFrame` 이 아예 돌지 않아 자동 재생을 검증할 수 없었다
   (슬라이더로 시각을 옮기는 경로는 전부 확인했고, 재생은 같은 `setTime()` 을 부른다).
   1배속에서 "1분 = 실제 1.4초" (`MS_PER_MIN`), 12분 전체가 약 17초다. 교실에서 보고
   너무 빠르거나 느리면 이 상수만 고치면 된다.
3. **화면 색이 실제로 어떻게 보이는지.** 스크린샷 도구가 이 환경에서 거의 동작하지 않아
   (미리보기 창이 표시되지 않으면 화면을 그리지 않는다) 색상값·대비는 `getComputedStyle` 로
   실측했지만 **눈으로 본 것은 개편 전 화면 한 장뿐**이다.

> 💡 **다음에 이 앱을 확인할 때 알아 둘 것** — 미리보기 창이 표시되지 않는 상태에서는
> **CSS 트랜지션이 시작값에서 멈춰 있다.** 그래서 `getComputedStyle(...).fill` 로 색을 읽으면
> 켜진 노드인데도 꺼진 색(`#414a7d`)이 나온다. 실제 값을 보려면
> `el.getAnimations().forEach(a => a.finish())` 를 먼저 부르거나 트랜지션을 잠시 끄고 읽을 것.
> (이걸 몰라서 "CSS 가 안 먹는다"고 한참 헤맸다.)

### 알려진 한계 (일부러 남겨 둔 것)
- **1·2차 관계망은 고정**이다. 1차 확산자 수나 연결을 학생이 바꾸는 기능은 없다
  (교과서 그림과 답이 달라지면 활동의 목적이 흐려진다). 바꾸는 실험은 "팔로워 수"로만 한다.
- **교과서 그림(2차)만 보는 화면은 없다** — 단계 탭을 하나로 합치면서 없앴다.
  대신 사람 모양의 숫자 1·2 와 고리 이름표가 교과서 부분을 가리킨다.
- **5차 이상은 그리지 않는다** (`MAX_STAGES = 4`). 2,688명이라 화면·성능 모두 무리다.
  5차 이후를 숫자로 보여 주던 누적 표도 2026-08-01 에 삭제했다.
- 3차 이상은 선 위의 "N분" 글씨가 없다(수백 개라 읽을 수 없다). 모두 1분으로 같기도 하다.
- **고리 이름표만 줌아웃 때 45px → 33px 로 조금 줄어든다** (`RING_MAX_K = 1.3`).
  「N분」과 사람 위 숫자는 완전히 고정이지만, 이름표는 크고 넓어서 1.3배가 넘으면
  그림 안 어디에도 놓을 자리가 없다 — 의도한 타협이다.
- 학생 답안은 저장하지 않는다 (개인정보 미수집 규칙).

### 다음에 할 일
1. 위 "확인하지 못한 것" 2가지를 실제 교실 PC에서 확인.
2. **화면상 글씨 크기(1차분 22px · 2차분 17px · 숫자 20/16px · 이름표 33~45px)가
   교실 TV에서 충분한지 확인.** 더 키우려면 위 "글씨는 줌아웃해도…" 표의 값들이
   서로 물려 있으니, 하나 바꾼 뒤 반드시 브라우저에서 겹침을 다시 잴 것.
3. 교과서 원본의 분 숫자를 확인할 수 있으면 `NETWORK` 를 실제 값으로 교체.
4. (요청 시) 교과서 2번 사이버 폭력 사례 표 추가 — 저장 없이 화면 표시만.

## 실행 방법
```
cd C:\project_AI\02.개인프로젝트\디지털문화\spread-simulator
npm install     (처음 한 번)
npm start       → http://localhost:8083
npm test        → 단위 시험 49개
npm run build   → dist/ 생성   (npm run preview 로 확인, 개발 서버를 먼저 끌 것)
```
외부 의존성이 없어서 `index.html` 을 더블클릭으로도 열 수 있게 만들었다
(`data-cleaner`·`excel-picker` 와 다른 점 — 그 둘은 SheetJS 를 import 하므로 서버가 필요하다).

## `.claude/launch.json` 을 고칠 때 (포트 8083)
이 앱은 launch.json 이 **세 곳**에 걸려 있다. 포트를 바꾸면 전부 고쳐야 한다.

| 파일 | `--prefix` 경로 |
|:---|:---|
| `C:\project_AI\.claude\launch.json` | `./02.개인프로젝트/디지털문화/spread-simulator` |
| `C:\project_AI\02.개인프로젝트\.claude\launch.json` | `./디지털문화/spread-simulator` |
| `C:\project_AI\02.개인프로젝트\디지털문화\.claude\launch.json` | `./spread-simulator` |

(Claude Code 는 **작업 폴더 기준으로** `.claude/launch.json` 을 찾기 때문에 이렇게 셋이 필요하다.
세 파일 모두 **UTF-8 로 저장**해야 한다 — CP949 로 저장하면 한글 경로가 깨져 `npm --prefix` 가 실패한다.)
